# tor
# taming-transformers


```bash
python3 -m venv ~/venv_taming-transformers
source ~/venv_taming-transformers/bin/activate

apt install libjpeg-dev

pip3 install -e ~/Code2/taming-transformers
pip3 install pytorch_lightning omegaconf pillow torchvision torchaudio einops
```